// #! 🪧 FPRINT JSX
// fonctions pour afficher des elements

//! FPrintConsole()
// affiche du texte dans la console
export function FPrintConsole(texte) {
  // Affiche le texte
  console.log(texte);
}